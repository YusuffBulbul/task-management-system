{{- if .Values.analyticsService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.analyticsService.deployment.name }}
  labels:
    app: {{ .Values.analyticsService.deployment.name }}
    app.kubernetes.io/name: {{ .Values.analyticsService.deployment.name }}
    app.kubernetes.io/component: backend
    {{- include "task-management.labels" . | nindent 4 }}
spec:
  revisionHistoryLimit: {{ .Values.global.revisionHistoryLimit }}
  replicas: {{ .Values.analyticsService.deployment.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.analyticsService.deployment.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.analyticsService.deployment.name }}
        app.kubernetes.io/name: {{ .Values.analyticsService.deployment.name }}
        app.kubernetes.io/component: backend
        {{- include "task-management.labels" . | nindent 8 }}
    spec:
      containers:
        - name: analytics-service
          image: "{{ .Values.global.imageRegistry }}/{{ .Values.global.imageNamespace }}/{{ .Values.analyticsService.image.repository }}:{{ .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.analyticsService.service.port }}
              protocol: TCP

          env:
            - name: SERVER_PORT
              value: {{ .Values.analyticsService.service.port | quote }}

            - name: SPRING_DATA_MONGODB_URI
              value: "mongodb://{{ .Values.mongodb.service.name }}:{{ .Values.mongodb.service.port }}/{{ .Values.mongodb.database.analytics }}"

            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              value: "{{ .Values.kafka.service.name }}:{{ .Values.kafka.service.brokerPort }}"

            - name: JAVA_TOOL_OPTIONS
              value: {{ .Values.analyticsService.javaOptions | quote }}

          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: {{ .Values.probes.springBoot.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.springBoot.readiness.periodSeconds }}
            failureThreshold: {{ .Values.probes.springBoot.readiness.failureThreshold }}

          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: {{ .Values.probes.springBoot.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.springBoot.liveness.periodSeconds }}
            failureThreshold: {{ .Values.probes.springBoot.liveness.failureThreshold }}

          resources:
            {{- toYaml .Values.analyticsService.resources | nindent 12 }}

          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL

      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.analyticsService.service.name }}
  labels:
    app: {{ .Values.analyticsService.deployment.name }}
    app.kubernetes.io/name: {{ .Values.analyticsService.deployment.name }}
    app.kubernetes.io/component: backend
    {{- include "task-management.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ .Values.analyticsService.deployment.name }}
  ports:
    - name: http
      port: {{ .Values.analyticsService.service.port }}
      targetPort: http
      protocol: TCP
  type: ClusterIP
  {{- end }}