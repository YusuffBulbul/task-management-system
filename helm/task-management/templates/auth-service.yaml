{{- if .Values.authService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.authService.deployment.name }}
  labels:
    app: {{ .Values.authService.deployment.name }}
    app.kubernetes.io/name: {{ .Values.authService.deployment.name }}
    app.kubernetes.io/component: backend
    {{- include "task-management.labels" . | nindent 4 }}
spec:
  revisionHistoryLimit: {{ .Values.global.revisionHistoryLimit }}
  replicas: {{ .Values.authService.deployment.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.authService.deployment.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.authService.deployment.name }}
        app.kubernetes.io/name: {{ .Values.authService.deployment.name }}
        app.kubernetes.io/component: backend
        {{- include "task-management.labels" . | nindent 8 }}
    spec:
      containers:
        - name: auth-service
          image: "{{ .Values.global.imageRegistry }}/{{ .Values.global.imageNamespace }}/{{ .Values.authService.image.repository }}:{{ .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.authService.service.port }}
              protocol: TCP
          env:
            - name: SERVER_PORT
              value: {{ .Values.authService.service.port | quote }}

            - name: SPRING_MONGODB_URI
              value: "mongodb://{{ .Values.mongodb.service.name }}:{{ .Values.mongodb.service.port }}/{{ .Values.mongodb.database.auth }}"

            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ required "jwt.existingSecret.name is required" .Values.jwt.existingSecret.name | quote }}
                  key: {{ required "jwt.existingSecret.key is required" .Values.jwt.existingSecret.key | quote }}

            - name: JAVA_TOOL_OPTIONS
              value: {{ .Values.authService.javaOptions | quote }}

          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: {{ .Values.probes.springBoot.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.springBoot.readiness.periodSeconds }}
            failureThreshold: {{ .Values.probes.springBoot.readiness.failureThreshold }}

          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: {{ .Values.probes.springBoot.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.springBoot.liveness.periodSeconds }}
            failureThreshold: {{ .Values.probes.springBoot.liveness.failureThreshold }}

          resources:
            {{- toYaml .Values.authService.resources | nindent 12 }}

          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL

      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.authService.service.name }}
  labels:
    app: {{ .Values.authService.deployment.name }}
    app.kubernetes.io/name: {{ .Values.authService.deployment.name }}
    app.kubernetes.io/component: backend
    {{- include "task-management.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ .Values.authService.deployment.name }}
  ports:
    - name: http
      port: {{ .Values.authService.service.port }}
      targetPort: http
      protocol: TCP
  type: ClusterIP
{{- end }}
